import { DirectiveTarefaConcluidaDirective } from './directive-tarefa-concluida.directive';

describe('DirectiveTarefaConcluidaDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveTarefaConcluidaDirective();
    expect(directive).toBeTruthy();
  });
});
